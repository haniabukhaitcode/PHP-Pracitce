//install
https://www.php.net/downloads.php 

Change environement path variable (admin) 

check path and download using terminal
echo %PATH%: check if php location exist (echo = return) 
php -v: shows the version installed

Start php server using terminal
php -S localhost:4000
